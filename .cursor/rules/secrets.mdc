---
description: Never commit secrets; use .env.example
alwaysApply: true
---

# Secrets

- Never commit API keys, `.env`, or Streamlit `secrets.toml` with real values.
- Document required vars in `.env.example` only.
- Prefer env / Streamlit secrets at runtime.
